info:
  name: Edit
  type: http
  seq: 4

http:
  method: PUT
  url: "{{PARH_URL}}/location/"
  body:
    type: json
    data: |2-
          {
            "city": "Copan",
            "street": "Calvario",
            "zipCode": "41102"
          }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Permite modificar los datos de una ubicación existente. Se usa el ID para identificarla y luego se actualizan sus datos.
