info:
  name: GetOne
  type: http
  seq: 1

http:
  method: GET
  url: "{{PARH_URL}}/location/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Muestra todas las ubicaciones guardadas en la base de datos. Es como una lista general de ciudades o lugares registrados.
