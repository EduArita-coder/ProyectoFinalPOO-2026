info:
  name: Delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{PARH_URL}}/location/"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Borra El Registro segun {id}
