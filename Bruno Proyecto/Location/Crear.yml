info:
  name: Crear
  type: http
  seq: 3

http:
  method: POST
  url: "{{PARH_URL}}/location"
  body:
    type: json
    data: |2-
          {
            "city": "Copan",
            "street": "Calvario",
            "zipCode": "41102"
          }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Se utiliza para registrar una nueva ubicación en el sistema. Se envían datos como la ciudad y el código postal.
